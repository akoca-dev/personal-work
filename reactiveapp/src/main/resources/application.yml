spring:
    profiles:
        active: dev
---
spring:
    profiles: dev
    data:
        mongodb:
            host: localhost
            port: 27017
            database: localdb
---
spring:
    profiles: preprod
    data:
        mongodb:
            host: localhost
            port: 27017
            database: localdb
---
spring:
    profiles: prod
    data:
        mongodb:
            host: localhost
            port: 27017
            database: localdb